import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import { LaTienditaApp } from './LaTienditaApp';

ReactDOM.render(
  <React.StrictMode>
    <LaTienditaApp/>
  </React.StrictMode>,
  document.getElementById('root')
);

